{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "uvu packages test",
    "build": "pnpm -r --filter=\"./packages\" --filter=\"!./packages/form-types\" exec -- bundt index.js",
    "clean": "rimraf pnpm-lock.yaml packages/*/node_modules packages/*/build packages/*/build.* node_modules"
  },
  "devDependencies": {
    "bundt": "github:saibotsivad/bundt",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "uvu": "^0.5.1"
  }
}
