{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "uvu packages test",
    "build": "pnpm -r --filter=\"./packages\" exec -- bundt index.js",
    "clean": "rimraf pnpm-lock.yaml packages/*/node_modules packages/*/build.* node_modules"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "uvu": "^0.5.1"
  },
  "dependencies": {
    "bundt": "github:saibotsivad/bundt",
    "rimraf": "^3.0.2"
  }
}
